# 变量与数据类型

## 📦 什么是变量？

变量就像一个**盒子**，可以用来存放数据。

```python
# 创建一个变量
name = "小明"
age = 18
```

**生活类比**：
- 变量名（`name`）就像盒子上的标签
- 变量值（`"小明"`）就是盒子里的东西
- `=` 表示"把右边的值放进左边的盒子"

## 🏷️ 变量命名规则

```python
# ✅ 正确的命名
my_name = "张三"
age2 = 20
_private = "私有"
userName = "李四"

# ❌ 错误的命名
2age = 20        # 不能以数字开头
my-name = "张三"  # 不能使用连字符
class = "一班"    # 不能使用关键字
```

::: tip 命名建议
- 使用有意义的名称：`age` 比 `a` 更好
- 多个单词用下划线连接：`user_name`
- 使用小写字母
:::

## 📊 数据类型

Python 有几种基本的数据类型：

### 1. 字符串（str）- 文本

```python
# 用引号包围的内容就是字符串
name = "Python"
message = '你好，世界！'
long_text = """这是
多行
文本"""

# 字符串操作
print("Hello" + " " + "World")  # 拼接：Hello World
print("Ha" * 3)                  # 重复：HaHaHa
print(len("Python"))             # 长度：6
```

### 2. 整数（int）- 整数

```python
age = 18
year = 2024
count = -5

# 整数运算
print(10 + 3)   # 加法：13
print(10 - 3)   # 减法：7
print(10 * 3)   # 乘法：30
print(10 // 3)  # 整除：3
print(10 % 3)   # 取余：1
print(2 ** 10)  # 幂运算：1024
```

### 3. 浮点数（float）- 小数

```python
price = 9.99
pi = 3.14159
temperature = -5.5

# 浮点数运算
print(10 / 3)   # 除法：3.3333...
print(0.1 + 0.2)  # 0.30000000000000004（浮点数精度问题）
```

### 4. 布尔值（bool）- 真/假

```python
is_student = True
is_adult = False

# 布尔运算
print(3 > 2)    # True
print(3 == 2)   # False
print(3 != 2)   # True
```

## 🔄 类型转换

有时候需要在不同类型之间转换：

```python
# 转换为字符串
age = 18
print("我今年 " + str(age) + " 岁")

# 转换为整数
num_str = "123"
num = int(num_str)
print(num + 1)  # 124

# 转换为浮点数
price = float("9.99")
print(price * 2)  # 19.98

# 查看类型
print(type("Hello"))  # <class 'str'>
print(type(123))      # <class 'int'>
print(type(3.14))     # <class 'float'>
print(type(True))     # <class 'bool'>
```

## 💬 格式化输出

有几种方式将变量插入到字符串中：

### f-string（推荐）⭐

```python
name = "小明"
age = 18
print(f"我叫{name}，今年{age}岁")
# 输出：我叫小明，今年18岁

# 可以在 {} 中进行计算
price = 10
quantity = 3
print(f"总价：{price * quantity}元")
```

### format 方法

```python
print("我叫{}，今年{}岁".format(name, age))
```

### % 格式化（旧式）

```python
print("我叫%s，今年%d岁" % (name, age))
```

## 🎯 输入用户数据

使用 `input()` 获取用户输入：

```python
# 获取用户输入
name = input("请输入你的名字：")
print(f"你好，{name}！")

# 注意：input() 返回的始终是字符串
age = input("请输入你的年龄：")
age = int(age)  # 转换为整数
print(f"明年你就 {age + 1} 岁了")
```

## ✏️ 动手练习

**练习 1**：自我介绍程序
```python
# 创建变量存储个人信息
name = "你的名字"
age = 18
city = "你的城市"
hobby = "你的爱好"

# 使用 f-string 输出
print(f"大家好，我叫{name}")
print(f"今年{age}岁，来自{city}")
print(f"我喜欢{hobby}")
```

**练习 2**：简单计算器
```python
# 获取用户输入
num1 = float(input("请输入第一个数："))
num2 = float(input("请输入第二个数："))

# 计算并输出
print(f"{num1} + {num2} = {num1 + num2}")
print(f"{num1} - {num2} = {num1 - num2}")
print(f"{num1} × {num2} = {num1 * num2}")
print(f"{num1} ÷ {num2} = {num1 / num2}")
```

**练习 3**：温度转换
```python
# 华氏温度转摄氏温度
fahrenheit = float(input("请输入华氏温度："))
celsius = (fahrenheit - 32) * 5 / 9
print(f"华氏 {fahrenheit}° = 摄氏 {celsius:.2f}°")
```

## 🎯 本节要点

| 概念 | 说明 | 示例 |
|------|------|------|
| 变量 | 存储数据的容器 | `name = "小明"` |
| 字符串 | 文本数据 | `"Hello"` |
| 整数 | 整数数据 | `18` |
| 浮点数 | 小数数据 | `3.14` |
| 布尔值 | 真/假 | `True` / `False` |
| f-string | 格式化输出 | `f"我是{name}"` |
| input() | 获取用户输入 | `input("提示：")` |

## 下一步

了解了变量和数据类型后，接下来学习运算符！

<div style="text-align: center; margin-top: 2rem;">
  <a href="/basics/operators" style="display: inline-block; padding: 12px 24px; background: linear-gradient(135deg, #306998, #4B8BBE); color: white; border-radius: 8px; text-decoration: none; font-weight: 500;">
    学习运算符 →
  </a>
</div>

